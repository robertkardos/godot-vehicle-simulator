extends SliderJoint


# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
#	var wheel = get_parent().get_path()
#	var carBody = get_parent().get_parent().get_node("RigidBody_Car").get_path()
#	set("nodes/node_a", carBody)
#	set("nodes/node_b", wheel)
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
